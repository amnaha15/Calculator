
public class Statistics {

	public static double getMean (double answer, double x){
		while (loop == true) {
			command = user.next();
			x = user.nextDouble();
			Basic.add(answer, x);
			Basic.divide
		
	}
}
